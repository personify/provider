should = require 'should'
provider = require '../domain/main'
require 'sugar'
Object.extend()

describe 'provider', ->

  it 'should provide dependencies', (done) ->
    throw new Exception 'not implimented'
    done()
